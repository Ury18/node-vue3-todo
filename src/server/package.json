{
    "name": "Affluence ToDo Server",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "sudo node src/index.js",
        "test": "mocha --timeout 10000 $(find src -name *.spec.js)",
        "test-debug": "mocha debug $(find src -name *.spec.js)",
        "test-coverage": "nyc --reporter=html --reporter=text mocha --timeout 10000 $(find src -name *.spec.js)"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "dotenv": "^16.0.0",
        "expect": "^27.5.1",
        "express": "^4.17.3",
        "mocha": "^9.2.2",
        "mongoose": "^6.2.7",
        "nyc": "^15.1.0"
    }
}
